/*  Start with the array you used in Exercise 11, but instead of just printing each person’s name,
    print a message to them. The text of each message should be the same,
    but each message should be personalized with the person’s name. */

let names = ["'Hamza'", "'Atif'", "'Faisal'", "'Shafqat'", "'Arham'"];

let message = "How are you and what you do now a days ?";
// for (let i = 0; i < names.length; i++) {
//     let friendName = names[i];
//     console.log(`Hi,${friendName},${message}`);
// }

console.log(`Hi,${names[0]},${message}`);
console.log(`Hi,${names[1]},${message}`);
console.log(`Hi,${names[2]},${message}`);
console.log(`Hi,${names[3]},${message}`);
console.log(`Hi,${names[4]},${message}`);
